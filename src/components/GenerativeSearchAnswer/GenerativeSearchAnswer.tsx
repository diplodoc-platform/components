import React from 'react';

import './GenerativeSearchAnswer.scss';

const GenerativeSearchAnswer: React.FC = () => {
    return <>Нейросеть подумала и не додумала</>;
};

export default GenerativeSearchAnswer;
