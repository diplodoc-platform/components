import {TextSizes, Theme} from './models';

export const DEFAULT_SETTINGS = {
    fullScreen: false,
    limitTextWidth: true,
    showMiniToc: true,
    theme: Theme.Light,
    textSize: TextSizes.m,
};
